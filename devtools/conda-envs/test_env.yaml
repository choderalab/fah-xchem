name: test
channels:
  - conda-forge
  - omnia
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - numpy
  - dataclasses-json
  - joblib
  - rich
  - seaborn
  - openmm
  - openmmtools
  - simplejson

    # Pip-only installs
  #- pip:
  #  - codecov

