name: test
channels:
  - conda-forge
  - defaults
  - omnia
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - numpy
  - dataclasses_json
  - joblib
  - rich
  - seaborn
  - openmm
  - openmmtools
  - simplejson

    # Pip-only installs
  #- pip:
  #  - codecov

